{
  "name": "@rootward/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Browser smoke test: beat one encounter through the real UI.",
  "type": "module",
  "scripts": {
    "test": "node smoke.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "playwright-core": "1.59.1"
  }
}
