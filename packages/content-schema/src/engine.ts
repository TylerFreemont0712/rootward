/**
 * The engine version content packs declare compatibility with (`engine_range` in pack.yaml). Bump the minor version
 * when the content format gains features packs may rely on, and the major version when old packs would break.
 */
export const ENGINE_VERSION = "0.1.0";
