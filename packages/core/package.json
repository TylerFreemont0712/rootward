{
  "name": "@rootward/core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, deterministic game engine: RNG, run events, encounter rules. No I/O.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./map": "./src/map/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@rootward/content-schema": "workspace:*",
    "zod": "4.6.4"
  },
  "devDependencies": {
    "yaml": "2.9.1"
  }
}
