# Warden

Wardens keep the Machine running while everyone else is busy changing it. They fight in a real terminal, inside a container that is broken on purpose, and a check script decides whether the fix holds. Where an Artificer writes a function, a Warden restarts a service, reads a log, and closes a port that should never have been open.
