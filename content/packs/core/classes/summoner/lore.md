# Summoner

Summoners bind models to useful work. Their fights are judged by evaluations, not by vibes: a prompt that passes its eval set, an embedding search that finds the right page, an agent that calls the right tool.
